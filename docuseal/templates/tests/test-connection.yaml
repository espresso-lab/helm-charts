apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "docuseal.fullname" . }}-test-connection"
  labels:
    {{- include "docuseal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "docuseal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
